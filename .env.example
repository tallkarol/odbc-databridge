# Database Configuration
# ODBC driver name - must match the driver installed in Dockerfile
# The Dockerfile installs odbc-mariadb which provides: MariaDB Unicode
# DO NOT change this unless you also update the Dockerfile
DB_DRIVER=MariaDB Unicode

# Database server address
# For Google Cloud MySQL: Use the public IP address or Cloud SQL Proxy
DB_SERVER=your-instance-ip-address

# Database name
DB_DATABASE=your-database-name

# Database username
DB_USERNAME=your-username

# Database password
DB_PASSWORD=your-password

# Database port (for MySQL: 3306, for SQL Server: leave empty or 1433)
DB_PORT=3306

# API Endpoints
# Zapier mock server endpoint for BirdEye service
BIRDEYE_ENDPOINT=https://hooks.zapier.com/hooks/catch/23151206/umyaaov/

# Zapier mock server endpoint for Example service
EXAMPLE_SERVICE_ENDPOINT=https://hooks.zapier.com/hooks/catch/23151206/umyaaov/

# Logging Configuration
LOG_DIR=logs
LOG_LEVEL=INFO
